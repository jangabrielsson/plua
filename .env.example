# Fibaro HC3 Connection Settings
# 
# Copy this file to one of these locations:
# 1. Project-specific: ./.env (current directory - takes precedence)
# 2. User-global: ~/.env (home directory - works from anywhere)  
# 3. Config directory: ~/.config/plua/.env (Linux/macOS) or %APPDATA%\plua\.env (Windows)
#
# DO NOT commit the .env file to version control!

# Required for HC3 API integration
HC3_URL=https://192.168.1.100
HC3_USER=admin
HC3_PASSWORD=your_password_here

# Optional: Additional environment variables for your scripts
DEBUG=false
LOG_LEVEL=info

# Example usage in Lua:
# local hc3_url = os.getenv("HC3_URL")
# local debug_mode = os.getenv("DEBUG") == "true"
